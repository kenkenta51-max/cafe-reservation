# STEP9 公開前検査

> 実施日：2026年7月31日
> 対象：`src/index.html`（全10セクション＋問い合わせフォーム）
> 方法：Chromium（Playwright）で `http://localhost:8000/src/` を実測
> 前回：`qa/step8_check.md`（実装時の検査）

---

## 1. 検出した不具合と対応

### 重大（公開前に必ず対応）

| No. | 内容 | 対応 |
|---|---|---|
| A-1 | **承認済み原稿にない文言が11箇所あった**（特徴の説明文3件、FAQ回答への加筆3件、問い合わせ本文、送信完了メッセージなど） | **修正済み**。すべて削除し `docs/06_copy.md` の文言に戻した |
| A-2 | **「2営業日以内に返信」がどの承認資料にもなかった** | **修正済み**。応答時間の記述を削除。対外的な約束は承認が必要なため |
| A-3 | **原稿にある「（就寝時にも配慮）」が実装から脱落していた** | **修正済み**。セクション4の静音項目に補記として追加 |
| A-4 | 購入CTA5箇所すべてが `href="#"` | **未対応（外部依存）**。Shopify商品ページのURLが未確定 |
| A-5 | CFキャプチャがプレースホルダのまま | **未対応（外部依存）**。掲載許可の確認待ち |

### 中

| No. | 内容 | 対応 |
|---|---|---|
| B-1 | `meta description` が90字で検索結果から溢れる | **修正済み**。82字に短縮 |
| B-2 | 法定表示リンク4件が `href="#"` | **未対応（外部依存）**。特商法表記は購入型LPで必須 |
| B-3 | og:image・og:url・canonical が仮URL | **未対応（外部依存）**。Shopifyの実URLが未確定 |
| B-4 | フォームがバックエンド未接続 | **未対応（外部依存）** |

### 低

| No. | 内容 | 対応 |
|---|---|---|
| C-1 | 未使用CSSクラス2件（`.message` `.mt-32`） | **修正済み**。削除 |
| C-2 | カラバリ写真の切り取り率25% | 未対応。1:1表示による意図的なトリミング。撮影素材の差し替え時に再確認 |
| C-3 | 1倍ディスプレイで画像解像度が表示幅の2.4倍 | 未対応。総転送量155KBのため影響は軽微 |

---

## 2. 再検査結果（修正後）

### 原稿との一致

| 項目 | 結果 |
|---|---|
| 照合件数 | 62件（`docs/06_copy.md` の見出し・補足見出し・本文・箇条書き・CTA文言） |
| 一致 | **61件** |
| 不一致 | 1件 — 「（問い合わせフォームへのリンク）」。原稿内の**指示文**であり掲載対象ではない |

うち14件は文言が一致し、表記構造のみ異なる（Q&Aを `<details>` に、スペックを表に組み替えたもの）。

### 表示・レイアウト

| 幅 | 横スクロール | 文字切れ | 見出しの折り返し |
|---|---|---|---|
| 1440px | なし | なし | 問題なし |
| 1280px | なし | — | — |
| 1024px | なし | — | 問題なし |
| 900px | なし | なし | 問題なし |
| 767px | なし | — | 問題なし |
| 390px | なし | なし | 問題なし |
| 320px | なし | — | 問題なし |

見出しは、各行の先頭が句読点・小書き文字にならないか、語尾が1文字だけ次行に残らないかを全幅で機械判定した。

画像切れ：カラーバリエーションの1枚のみ切り取り率25%（1:1表示による意図的なトリミング）。他4枚は切り取りなし。

### CTA・リンク

| 項目 | 結果 |
|---|---|
| 購入CTA | 5個（本文4箇所＋スマホ固定CTA）。文言は全箇所「NAGIを購入する」で統一 |
| ページ内アンカー | 3件すべて到達可（`#main` `#contact` ×2） |
| リンク切れ | なし |
| 遷移先未設定（`href="#"`） | 10件 → 未完了事項へ |

### フォーム

| 項目 | 結果 |
|---|---|
| 空送信 | 氏名・メールにエラー表示、`aria-invalid="true"`、最初の不正項目へフォーカス移動 |
| 不正メール | 形式エラーを表示。入力済みの他項目は保持 |
| 正常送信 | フォームを閉じ完了メッセージを表示、`role="status"` へフォーカス移動 |
| ラベル | 全項目 `<label for>` 対応（未対応ゼロ） |

### アクセシビリティ

| 項目 | 結果 |
|---|---|
| `h1` | 1個。見出し15個の階層に飛びなし |
| `alt` | 画像5枚すべて設定済み（空altなし） |
| コントラスト比 | **基準未達ゼロ**（本文4.5:1／大きな文字3:1で全テキスト要素を判定） |
| キーボード操作 | Tab順序が視覚順と一致。全要素でフォーカス表示あり。FAQはEnterで開閉 |
| タップ領域44px未満 | 1件（本文中のインラインリンク）。WCAG 2.5.8 の inline 例外に該当 |
| `prefers-reduced-motion` | 対応 |
| JS無効時 | 本文が読める（`.reveal` の不透明度=1）。FAQも開閉可 |

### メタ情報

| 項目 | 値 |
|---|---|
| title | 34字 |
| description | 82字 |
| OGP | 10件（type / site_name / title / description / url / image / image:width / image:height / image:alt / locale） |
| Twitter Card | `summary_large_image` |
| canonical | 設定済み（URLは仮） |
| JSON-LD | Product構造化データ（価格・在庫・送料・返品ポリシー） |
| favicon | SVG＋PNG 32px＋apple-touch-icon 180px |

### 表示速度・ビルド

| 項目 | 結果 |
|---|---|
| 総転送量 | **155KB**（全画像読込後） |
| 初期リクエスト | 3件（HTML＋FVの商品写真＋favicon） |
| 画像形式 | WebP（JPEGフォールバック付き）、FV以外は遅延読込 |
| コンソールエラー | なし |
| 4xx/5xx | なし |
| HTML構文 | タグの開閉不一致なし、重複IDなし |
| 未使用CSS | なし（修正後） |

---

## 3. 未完了事項

### 外部サービス・素材に依存（当方では完了できない）

| 項目 | 必要なもの | 影響 |
|---|---|---|
| **購入CTAの遷移先** | Shopify商品ページのURL | **未設定のまま公開すると購入導線が機能しない** |
| **CFキャプチャ画像** | 掲載許可の確認と実物のキャプチャ | 実績数値（512名・8,942,000円）の**出典を示せない状態** |
| **特定商取引法に基づく表記** | 掲載ページのURL | 購入型LPでは**法令上必須** |
| 個人情報保護方針 | 文面（8/20納品予定）とURL | フォームからの導線が機能しない |
| 会社概要 | 掲載ページのURL | — |
| フォーム送信 | Shopifyフォームのエンドポイント | 現状は完了表示のみで**送信されない** |
| og:url・og:image・canonical | Shopifyの実URL | SNS共有時にOGP画像が表示されない |
| カラバリ2色の写真 | ミストグレー・サンドベージュの撮影 | プレースホルダのまま |
| PSEマークの認証番号 | 確定値 | 法定表示が未完成 |
| Noto Sans JP | self-host用woff2 | 現在はシステムフォント。差し替え時に行間・字送りの再確認が必要 |
| 商品写真の背景透過素材 | 切り抜き済みPNG | 現在は写真パネル扱い |
| GA4・Meta Pixel | 計測タグ | 未設置。公開時にShopify側で設定 |

### 当方で未確認の項目

| 項目 | 理由 |
|---|---|
| **実機での表示** | 検査はChromiumのエミュレーションのみ。iOS Safari・Android Chromeの実機確認が未実施 |
| **Safari・Firefox での表示** | Chromium系以外のブラウザで未確認。`text-wrap: balance`・`aspect-ratio`・`env(safe-area-inset-bottom)` は挙動差が出やすい |
| **本番環境での表示速度** | ローカル配信での計測。CDN・Shopifyのテーマ読込・外部スクリプトの影響は未反映 |
| **Shopifyページ機能への組み込み** | テーマ側のCSSとの衝突は組み込み後に要確認 |
| **メール到達** | フォーム未接続のため、受付完了メールの文面・到達は未検証 |
| **原稿の事実確認** | 支援総額、電気代の測定条件、スペックの量産版整合など `docs/06_copy.md` 末尾の5件はクライアント確認待ち |

---

## 4. 修正の記録

このQAで `src/index.html` に加えた変更：

1. 特徴の説明文3件を削除（承認済み原稿にない加筆）
2. FAQ回答3件を原稿どおりに戻す（月額目安・同梱物・Wi-Fi要件の加筆を削除）
3. 「2営業日以内」の応答時間の記述2箇所を削除
4. 「（就寝時にも配慮）」を追加（原稿からの脱落を補填）
5. `meta description` を90字→82字に短縮
6. 未使用CSSクラス `.mt-32` `.feature__d` を削除
